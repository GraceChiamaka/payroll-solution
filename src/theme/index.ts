import { colors, media, fontSize, fontFamily, borderRadius, border, spacing } from './declarations'

const theme = {
  colors,
  media,
  fontSize,
  fontFamily,
  borderRadius,
  border,
  spacing,
}

export default theme
