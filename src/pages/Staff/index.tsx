import Layout from "../../components/Layouts";
import { useLocation, matchPath } from "react-router-dom";

const Staff = () => {
  //   const path = matchPath("/staff/:id", {});
  //   console.log(path);
  return <Layout>Stadd</Layout>;
};
export default Staff;
